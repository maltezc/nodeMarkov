const { MarkovMachine } = require("./markov")

describe("Markov tests", function() {


  let testSentence;

  beforeEach(function() {
    testSentence = "The quick brown fox jumped over the lazy dog and also jumped over the slow turtle"
  })

  // chains
  test("Returns map of words", function () {

  })


  // get text
  test("Returns map of words", function () {

  })
})